{
  "name": "axiom-monorepo",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "dev:ui": "npm run dev --workspace=packages/ui",
    "dev:desktop": "npm run dev --workspace=packages/desktop",
    "build:ui:desktop": "npm run build --workspace=packages/ui -- --mode desktop",
    "build:desktop:main": "npx esbuild packages/desktop/src/main.ts --bundle --platform=node --target=node18 --outfile=packages/desktop/out/main/main.js --external:electron && npx esbuild packages/desktop/src/preload.ts --bundle --platform=node --target=node18 --outfile=packages/desktop/out/preload/preload.js --external:electron",
    "dist": "npm run dist --workspace=packages/desktop",
    "setup:python": "node packages/desktop/scripts/download-python-runtime.js",
    "clean:dist": "rm -rf packages/desktop/dist packages/desktop/resources/python-runtime packages/desktop/renderer packages/desktop/out",
    "release:mac": "npm run clean:dist && npm run setup:python && npm run build:ui:desktop && npm run build:desktop:main && npm run dist",
    "start:desktop": "pkill -f 'python3.*main.py.*18432' || true && npx electron packages/desktop"
  },
  "devDependencies": {
    "7zip-bin": "^5.2.0",
    "electron": "^28.2.0"
  }
}
